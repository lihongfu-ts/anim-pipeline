# 一个人用 AI 做 2D 角色动作动画的完整管线

> 从一张立绘到游戏里能打的技能。全部结论来自一个真实项目的实测，
> 包含**五条走死了的路**和**每一笔花掉的钱**。

**验证环境**：Cocos Creator 3.8.8 · 横版带纵深的动作游戏 · Q 版 3.5 头身 · 游戏内角色约 96×128 px

**已产出**：走路、待机、三段普攻、崩山击、后跳、小怪（走/攻/死）、大招——全部接进游戏并通过无头验收

---

## 〇 先说结论：为什么是这条路

### 走通的唯一一条

```
一张立绘  →  图生视频  →  抽帧  →  抠图  →  对齐  →  序列帧图集  →  接进游戏
```

**它能成立的原因只有一个：从头到尾没有"拆过图"。**

裁切、抠图、缩放、对齐，全部是**不需要理解画面内容的机械操作**。
角色的头发怎么飘、刀怎么划出弧线——那是模型在演，不是你在算。

### 走死了的五条（别重走）

| 路线 | 为什么死 |
|---|---|
| 骨骼动画（自己绑） | 试过，不行 |
| 骨骼换皮 | 找不到「人形 + 带 run + slot 数适中」的现成 spine 素材（量过一个妖兽骨架：17 个 slot 全是 mesh，只有 attack/start/wait） |
| 分层 + 程序化 | 和骨骼动画是同一件事，只是层数不同。难点都在「把一张厚涂图正确拆开」+「拆完补洞」 |
| 8 方向 / 装备可见 | FLARE（暗黑 like）实测：7 层 × 8 方向 × 32 帧 = **1792 张**，还要逐方向写遮挡表。一个人做不完 |
| 纯变换动画（不出帧） | 图是静态的，大动作必露馅——手不动，刀在转 |

**共同点**：前四条的成本都压在「理解并拆解画面内容」上，而这恰恰是最贵、最容易做坏的一步。
走通的那条路把这一步**整个绕开了**。

---

## 一 · 源图：整条链最重要的一张图

后面每一步的质量上限都由这张图决定。它出问题，后面全白做。

### 1.1 唯一源图原则

**出任何动画之前，先确认源图是哪一张。**

这是整个项目最贵的一条教训：技能用了 A 图，走路/待机用的是 B 图，
两张图的**站姿不同**（A 是刀斜垂向左下，B 是刀横在身前）。

结果：技能收招回 A 姿势、待机是 B 姿势 ⇒ **刀会瞬移一次**。
三发 720P 视频（¥6.3）全部作废，已完成的技能也要重出。

> 病根不是亮度（两张只差 4~9 点，肉眼看不出来），是**姿势**。
> 而且首尾帧锁得越准，这个跳越明显——因为它把尾帧精确钉在了错的姿势上。

**核查办法很便宜**：直接比 md5；或比对视频首帧与立绘的「占比 / 宽高比 / 高光」。
定完之后，废弃的那套立刻移进 `_废弃/`，别留在手边。

### 1.2 立绘本身的三条

**① 刀（或任何薄片道具）必须横在身前、面正对观众**

刀斜向下贴身时，摆臂会把它转到正对镜头——薄片没有面积，转过去就"消失"了。

> 这一条我们一开始判断反了。当时的理由是「刀贴身所以模型不容易乱改」，
> 实际恰恰相反。

**② 立绘 prompt 里别写"雾气""光晕"这类描述**

`faint ghostly grey mist curling off it` 会被出图模型画成**一道实心的、和刀刃同形的弧**。
然后视频模型忠实地把它演成第二把刀。

> 「角色有两把刀」的根因在立绘，不在视频。查视频模型是查错了方向。

**③ 底色要从图里量出来，别猜**

出图时给的是 `#9A9A9A`(154)，实际落到文件里、再过一遍视频压缩，
变成 `[137,134,135]` 且**不再是中性灰**。抠图按猜的值走一定失败。

### 1.3 留白图：技能动画的必需品

技能动画**不能用满画幅的立绘**当源图。

因为提示词里那句必须有的构图约束（见 §2.1）说的是「大小和构图与原图完全一致」，
如果原图角色占满画面，这句话等于锁死了「占满画面」——**刀一展开必然出画**。
留白句写多少遍都摁不住。

```
满画幅立绘   →  四边全干净的帧   22 / 125
留白图       →  四边全干净的帧  146 / 150
```

**只换了源图，提示词一个字没改。**

留白图规格：1024×1024，角色占 **55%**，底色照抄立绘量到的值。

---

## 二 · 提示词：三条铁律 + 五条推论

### 2.1 每条提示词开头必须有的两段

**构图约束**（不写会被推近成半身，脚全没了，脚底对齐直接作废）：

> 必须保持人物全身完整地在画面内，从头顶到双脚全部可见，双脚下方留出空白，
> 绝对不要推近、不要放大人物、不要裁掉腿和脚。人物在画面中的大小和构图
> 自始至终与原图完全一致。

**别写转速**：

转速在游戏里是**播放帧率**决定的，跟视频没关系。
视频转快只有坏处：一圈的帧数变少（没得挑）+ 每帧糊（抠图边缘也糊）。

该要的是**姿态**：「长发被离心力甩得完全向外张开、飘成水平；衣摆和腰带被甩起来向外扬；
身体略微前倾，重心压低；刀带拖影。」

### 2.2 三条铁律（前 7 次生成换来的）

**① 把该静止的部位一个个点名**

身体 / 头部 / 面部 / 四肢 / 躯干 / 衣服主体——只说「保持原位」不够。

**② 该动的要单独放开，并给幅度**

「幅度清晰可见」「像水草一样摆动」。
只写「不许动别的」会把该动的一起摁死。

**③ 用禁止句，别用强调句**

| 写法 | 结果 |
|---|---|
| 「保持发光亮度一致」 | ❌ 整个人发光崩掉 |
| 「严禁发光变白」 | ✅ 管用 |
| 「刀上雾气飘动」 | ❌ 雾炸成一团吞掉角色 |

### 2.3 五条推论

**③a 禁属性 ≠ 禁动作**

锁「刀的形状 / 数量 / 颜色」不伤运动。
锁「手臂不许甩」会**外溢到全身**——实测腿部形变从 30% 掉到 7%。

**③b 禁止句要单独成段，别混在一长串名词里**

```
❌ 「躯干、肩膀、腰、胯、双腿、双脚、双臂…全部保持原位不动」 → 腿照动 4.89%
✅ 要摁死哪里，就给那里单独一串禁止句
```

权重会被稀释。这一条在后面每一轮都重新验证过。

**③c 要姿态，别要成因**

写「脚下有气流向上吹」→ 模型读成"有风"，风向取了水平 → **头发向后平飘**。
改成「每一缕头发笔直朝向画面正上方、发梢指向画面顶边」→ 才管用。

> 这和「别写每秒转几圈」是同一条规律：**描述你要看到的结果，不要描述物理原因。**

**③d 别在提示词里写「砸在地面上」**

如果你同时禁止了画地面，这就是自相矛盾——模型会给你画一块地。
只描述**运动方向**：「向前下方全力砸下，砸到最低处停住」。

**③e 「镜头固定」≠「人物固定」**

必须分成两句写。合成一句会把人一起摁死。

### 2.4 一条提示词修不了的：姿势结构

想让角色"吼"，但下半张脸被面罩遮着——模型为了做"抬下巴"会**直接把面罩扒了**，角色当场崩。

⇒ 用「头猛地后仰 + 下巴抬起」表达，不画嘴。
真要嘴，得先改立绘。**那是另一件事，不是提示词能修的。**

---

## 三 · 出视频：三家模型的分工

### 3.1 分工表（最重要的一张表）

| 场景 | 用哪家 | 成本 |
|---|---|---|
| 身体静止型（idle，只有头发衣摆在飘） | **GLM** (cogvideox-flash) | 免费 |
| 大幅度身体动作 **+ 角色必须保持** | **万相** (wan3.0-video) | ¥1.05 / 5s 480P |
| 大幅度身体动作，角色崩了也无所谓（探姿势） | **ark** (seedance) | 免费额度 |

### 3.2 这张表背后的判据

同一条提示词（蹲下蓄力 → 举刀过头 → 头后仰 → 头发竖起）打三家：

```
GLM       ❌ 做不出大动作
ark       ✅ 动作全对（蹲下、举刀、仰头、末帧保持住）
          ❌❌ 角色全崩：面罩没了 · 发型变马尾 ·
             刀从宽刃弯刀变成细长直剑（带十字护手） · 身材拔高
万相      ✅ 动作全对 + ✅ 角色全保住（面罩在 / 发型在 / 三头身在）
          ✅ 而且头发真的竖起来炸开了 —— 前两家都没做到
```

**关键在这里**：ark 打了两发，第二发**加了一整段角色一致性禁止句**
（严禁改发型 / 严禁摘面罩 / 严禁改身材比例 / 严禁刀身变形），
而**崩坏方式一模一样**。

> ⇒ **seedance 做大幅度动作时，是在"重新生成一个角色"，不是在变换原图里那个。**
> 这是模型能力问题，提示词修不了，再抽多少发都一样。

同一条提示词里那堆"刀高"否定句（腰/胸口/肩膀/过头顶/手臂前伸/平举，一条条单独禁），
**在万相上生效、在 seedance 上不生效**。

万相真正的强项不是画质好，是**保持原图里那个角色**。
（头对头数据：身高漂 1.001；智谱打 12 发没稳住的刀，它第一发就稳。）

### 3.3 头对头数据（同一张立绘、同一套提示词）

| | 智谱 cogvideox-3 | 万相 wan3.0-video |
|---|---|---|
| 循环缝（基准 0.99） | 1.54 ❌ | **1.02** ✅ |
| 亮度漂（上限 4.17） | 5.2 ❌ | **3.5** ✅ |
| 身高漂 | 1.097 | **1.001** |
| 刀形态（面积变异） | 5.93% | **3.91%** |
| 腿部动作（前 1/3） | 25.73% | **32.33%** |

### 3.4 `--last` 首尾帧：连招衔接的地基

**「最后回到和开头完全相同的站姿」这句话在提示词里写了三遍，模型一次都没听。**

⇒ 正确做法：`--img` 和 `--last` 给**同一张图**，首尾帧就一模一样。

后果是决定性的：所有动作片段的首尾都是同一个站姿
⇒ **任意两段之间、以及接走路/待机，都不需要过渡帧。**
⇒ 加新动作（后跳/受击/死亡）= 出一发新的，已有的一个字都不用改。

> 万相和 seedance 都支持，**但三家三种写法，别互相抄**：
> 万相用 `media[].type`；火山用 `role: first_frame/last_frame`
> （要 `doubao-seedance-1-0-pro-250528`，pro-fast 不支持 flf2v）。

### 3.5 位移动画：靠"右移尾帧"钉，提示词说不动它

想做后跳（角色要真的往右移动）：

| 做法 | 最右 Δx（占身高） | 结果 |
|---|---|---|
| `--img`/`--last` 同一张（人都在中央） | 4.3% | ❌ 原地垂直跳 |
| `--last` 换成**右移版留白图**（cx=0.74） | **36.0%** | ✅ 且结尾停在右边不回来 |
| 同提示词、不带 `--last` | 55.6% | ❌ 姿势乱飞，收招停在别的姿势 |

钉的目标是 36.6% ⇒ **位移精确等于首尾两张图的位置差**。

**病根**：中性灰底上没有参照物，「向右退半个身高」对模型不是可验证目标。
而 `--last` 同图 = 明说「最后回到这个位置」。

> ⚠ **钉得越远，它越要先往左跑一段助跑**：钉 0.80 那发助跑 −49%，钉 0.74 只有 −22%。
> **0.74 是工作点，别贪。**

### 3.6 万相的两个坑

**① API 域名必须是「独立业务空间」专属域名**

```
✅ https://ws-xxxxxxxx.cn-beijing.maas.aliyuncs.com/api/v1
❌ https://dashscope.aliyuncs.com/api/v1     ← 公共域名，恒 401
```

栽了两次。两次报的都是 401 且提示「key 格式不对」——**极容易误判成 key 本身有问题**。
正确域名在控制台导出的 apiKey CSV 里，字段名 `dashScope`。

**② 背景噪点是智谱的 3 倍，会让抠图静默失效**

```
万相背景噪点 ±13/通道（智谱只有 ±4）
⇒ 色度距离 d≈22，正好落进抠图的软阈值区间(16~52)
⇒ 背景残留 alpha≈43 的椒盐噪点（不成片，调阈值压不掉，16→36 全无效）
⇒ 内容盒被撑满整个画面
⇒ 量到的"身高"恒等于画面高（打印出来是「1175 1175 1175」六帧一模一样）
⇒ 归一化系数全错，切出来的角色身高在 157~202 之间乱跳
⇒ 一行报错都没有
```

**解法**：分开噪点和角色的**不是行像素数，是 alpha 值本身**——噪点 43，实体 255。
卡 `alpha > 200` 一刀切。

> 按「行像素数占列带宽 4%」筛过一版，不行：噪点密度逐帧变，一半帧照样过线。

### 3.7 其它接口要点

```
① 异步靠请求头  X-DashScope-Async: enable
② 任务 id 在 output.task_id（不是顶层 id）；轮询 GET /tasks/{id}，状态 SUCCEEDED
③ 图生视频用 input.media 数组（不是 input.img_url）；本地图转 base64 data URL 实测可用
④ 默认出有声视频 ⇒ parameters.audio=false 关掉
⑤ API 输出不打水印（网页版那个移动水印是网页端加的，裁不掉、会切脚）
```

**任务 id 必须立刻落盘。**
起因：`... | tail -6` + 中途杀进程 ⇒ id 丢了、片取不回、**钱照扣**。

---

## 四 · 视频 → 序列帧图集

### 4.1 六条铁律（全是被指出后量出来的）

**① 脚底不是内容盒底部**

刀尖伸到 y=0.884，比脚（0.879）还低。
⇒ 脚底 = **中间 40% 列**的最低不透明行。

**② 水印裁底部一条，不要中心裁**

实测在 (0.85, 0.96)，裁 y>0.895 即可。位置随厂商/模型变，**裁完必须自检**。

> 而且继续裁会切脚：3/4 侧立绘脚底到 y=0.934，裁 0.895 是从脚踝横切。
> ⇒ 裁切前先量脚底，会切到角色就**直接退出**。

**③ x 对齐用「头部重心」，不用脚底中心**

破烂衣摆的锯齿一抖，锚点就抖。
实测头部差 6.2px，而脚底/头顶/内容盒**全是对齐的**——只有头部能分辨出来。

> 自转动画例外，用内容盒中心（bbox）。

**④ 缩放基准用「核心区面积」**

```
❌ 不用身高    含纵向伸展，等比缩放把横向缩窄 4.4% = 抖
❌ 不用肩宽    长发垂到肩膀会撑大 7.1%
✅ 腰腹那块实心区    没有会飘的东西 ⇒ 对头发和刀免疫
```

**⑤ 底色要从图里量**（见 §1.2③）

**⑥ 判断周期性动作，先量周期再采样**

用 15 帧间隔采样一个 8~12 帧周期的动作 ⇒ **混叠** ⇒ 误判「刀根本没转」。
用自相关量周期，几行代码。

### 4.2 三种挑帧模式

| 模式 | 用于 | 要点 |
|---|---|---|
| `still` | 待机 | 脸/躯干/腿**三区各算各的、取 max**（塞一个框求平均会把腿脚稀释掉） |
| `loop` | 走路、旋转 | 自动找循环点，见下 |
| `even` | 攻击、死亡等一次性动画 | 均匀采样 |

**`loop` 模式踩过的三个坑**：

```
① 目标函数一开始是反的
   「首尾差最小」在专挑静止段（静止 4.49 vs 真迈腿 12.00）
   ⇒ 先按逐帧运动量筛掉静止段

② 光筛静止段还不够：走路左右腿对称 ⇒ 半周期首尾同样接近
   自由搜段长会选中半周期（0.52s，真周期 1.52s）
   ⇒ 循环播放时同一条腿一直在前，看着是瘸的
   ⇒ 用自相关量周期，段长钉死在周期上

③ 段内要允许 ≤15% 低活跃帧
   要求"一帧不落全活跃"会找不出任何段
```

### 4.3 身体静止：靠合成冻结，别指望模型

待机动画的病根是模型**摁不住腿**。同一条 prompt 打三发，
成品腿脚剪影变化 **2.75% / 10.39% / 16.69%**（差 6 倍，上限 1.5%）。

这是**方差**，不是均值——提示词修不掉。

> 已排除对齐的嫌疑：两帧头部重心只差 1.50px、头顶 y 差 0，
> 但**脚底水平中心差 18.5px** ⇒ 脚是真在挪。

**解法**：两帧本来就对齐好了 ⇒ 把后续帧的核心框**整块换成首帧的**。

```
freeze=body   框内(脸+躯干+腿)=首帧  ⇒ 身体剪影变化数学上恒等于 0
freeze=legs   只钉死腿脚、放开躯干    ⇒ 要"呼吸感"时用这个
```

⚠ 别拿它处理走路——那是身体必须动的动画。

### 4.4 切图参数

```
--at=<手点的帧号>        技能动画必须手点，别自动挑
--scale=<站直身高>       ⚠ 必须量这一条视频自己的（通常是 f1，中间 40% 列的 foot−top）
                        别省：默认拿首帧当基准，而技能首帧常是最深蹲那一帧
--target=202            固定值 ⇒ 所有 clip 的角色一样大
--cell=WxH / --footpad  每个技能重新量（先用超大格子 probe 一版，看四向最大需求）
--norm=off              自转类动画关掉归一化
```

---

## 五 · 验收：判据必须是区间

### 5.1 基准从已出货的游戏量，不是自己拍脑袋

基准 = `Slash-The-Hordes`（538★，已发布的 Cocos 割草游戏）主角的 Idle / Move / Die。

| | 动量 | 形变 | 循环缝 | 剪影密度 | 亮度漂 |
|---|---|---|---|---|---|
| 基准 Idle | 40.43 | 13.20% | 1.00 | 73.6% | 0.2 |
| 基准 Move | 48.08 | 17.79% | 0.99 | 70.0% | 1.4 |
| 基准 Die | 69.88 | 40.19% | 1.21 | 63.9% | 9.9 |

### 5.2 只有下限的判据是假的

一个「角色整个发白」的废片跑出动量 99.9 / 形变 35.9——**只看下限的话全部"达标"**。

⇒ 必须加**崩坏检测**。「亮度漂」：正常动画（哪怕是死亡）≤ 9.9，一崩跳到 72.8。

### 5.3 第五项指标：尺度无关的剪影面积变异

```python
a = alpha > 128
h = 内容盒高
area = a.sum() / (h * h)      # 除以 h² ⇒ 对位移和缩放免疫
指标 = area 的变异系数（std / mean）
```

前四项指标（动量/形变/循环缝/剪影密度）**同时漏掉了两种崩坏**：

```
「刀消失」        四项都正常 —— 但面积会掉    实测 7.34%（正常 1.86~3.14%）
「头发糊成灰块」  四项都正常 —— 但面积会涨    实测 7.83%
```

**原理**：任何「该有的东西没了」或「多出一块不该有的」都会在面积上留痕，
而位移和姿势变化不会。

> ⚠ 别用「剪影异或」量刀的稳定性——它把**刀在甩**（好）和**刀在变形**（坏）
> 混成一个数，数值高低跟好坏**不是单调关系**。实测排序和肉眼完全相反。

### 5.4 指标看不出来的那一类：肉眼必查

**「刀有没有变成一条细白线」**——抬手/收招段模型爱把刃口转向镜头，
薄片没面积，指标全正常。（一段技能的 f25~f50 整段就是这么废的。）

> 后来做了专门的逐帧检测工具。两个坑：基准不能用全片中位数（静止姿势会被误报）；
> 阈值不能写死也不能逐帧算（用全片身高中位数 ×0.17）。

---

## 六 · 出一个技能的完整配方

照着走就行。

```
① 源图    用留白图（角色占 55%），确认是唯一合法源图（md5 核对）

② 提示词  四段结构；镜头固定 ≠ 人物固定，分开写；
          只描述运动方向，别描述与其它约束冲突的场景元素

③ 出片    大幅度动作直接上万相，别过 GLM
          一次一发，不散弹；每一发都先确认
          抽卡探姿势才用 ark（免费）

④ 挑帧    先出带帧号的 12 格联络表看整段，再逐帧量四边贴边
          肉眼必查「刀变细白线」
          重心放在命中那一段，别挑成「举刀×3 + 命中×1」

⑤ 切图    --at 手点帧号
          --scale 量这条视频自己的站直身高
          --target=202 固定
          --cell / --footpad 每个技能重新量

⑥ 接线    图集属性 + 逐帧时长表(HOLDS) + 时间轴常量
          复用 playOnce / setLift / applyLunge / strike
```

### 6.1 接线端的四个可复用零件

**`playOnce(frames, holds, footTrim, onDone)`**
- **逐帧时长**，不是统一帧率——打击感全在顿帧上
- **per-clip footTrim**：技能格子和走路格子尺寸不同，共用一个值会偏 91.5px
- 用**累计时间找帧**，不是「计时器减帧时长」（逐帧不等长时那样写会漂）

**`setLift(px)`（腾空）**
- 抬**贴图**，不动角色节点——角色节点的 y 是 Y 排序键 + 判定纵向基准
- 同时缩影子：横版里「跳起来了」几乎全靠影子读出来

**`Locked` / `applyLunge(dx)`（位移）**
- 锁的是**输入**不是速度（残余速度照常滑停，朝向自动保持）
- 位移**直接改位置**不改速度（锁住时目标速度是 0，塞进去下一帧就被拉没）
- 用**累计位移之差**推进 ⇒ 总距离不受帧率影响

**`Hittable` / `ImpactFx`（受击）**
- 后坐**只动 x**，动 y 会让 Y 排序闪烁
- 后坐**取较大值不累加**，否则第三下把木桩推飞
- update 要有 init 前空守卫——组件 update **不保证**排在调用方 init() 之后

### 6.2 一个真实的验收数据长什么样

```
前冲      96.0px（目标 96）      腾空 91.5px（目标 90），峰值 87ms（目标 100ms）
影子      1.0 → 0.58 → 1.0（目标 0.58）
逐帧时长  70/70/60/260/90/180ms  —— 和 HOLDS 表一模一样，顿帧确实停住了
落地      188~205ms；砸中帧开始于 205ms
          ⇒ 落地/砸中/顿帧/首段判定四件事同一时刻（设计如此，差开就散）
三段判定  205 / 271 / 354ms 各闪白一次
误伤      只有目标那根柱子被打中，另外三根没反应 ⇒ 判定框对
报错      0
```

> ⚠ 验收脚本**别用逐点采样**——每次往返几十毫秒，22 个采样点只覆盖到腾空段第一帧，
> 差点误判成「腾空根本没生效」。
> ⇒ 改成在页面内逐帧记进数组，跑完一次性读回。

---

## 七 · 连招：比单发技能多出来的四个问题

**① 动画机只支持一个 one-shot**

`if (animator.IsBusy) return` 会把连招第二段直接吞掉。

⇒ 要加「连招窗口 + 输入缓冲」：在后半段按键**记下来**，播完立刻接下一段。

> ⚠ 别做成「随时可打断」——连点会把每段都掐在第一帧，看着像抽搐。

**② 段间衔接靠 `--last`，不靠挑帧运气**

把每段的尾帧钉死成同一张 ⇒ 三段首尾都回到同一个姿势 ⇒ 衔接天然顺。（见 §3.4）

**③ 格子尺寸每段重新量**

别抄上一个技能的。footpad 一变，per-clip trim 就得跟着给对。

**④ 判定要分段给**

每段的 reach / depth / 命中时刻都不一样 ⇒ 把写死的静态常量收进一张**招式表**：

```
一段 = 帧图 + holds + hits + cancelFrom + lunge + reach + footTrim
```

### 7.1 三段普攻返工了两轮，教训比结果值钱

反馈了三件事，每一件都指向同一类错误：**在原始视频上量方向**。

**① 「边攻击边冲」** ⇒ lunge 24/24/48 改成**全 0**

> **代码位移只能补动画里已经有的位移，不能凭空造。**
> 实测这三条视频横向位移不到 10 世界像素 ⇒ 给 0。
> 另一个技能给 96 是因为它视频里**真的跃了出去**。

**② 「偶尔出现刀向后的一帧」**

第一次判错了：拿「离躯干中线最远的前景点」在**原始视频**上量，
把左侧一层很淡的运动模糊算成了刀。

> ⇒ **方向必须在切出来的图上量**（抠底会去掉那层模糊）。

规则：**命中帧的刀必须指向角色面朝的方向；只有蓄力帧可以朝后。**

> 一度矫枉过正、把所有朝后的帧都删了 ⇒ 连蓄力一起没了 ⇒ 没有挥砍感。

**③ 「我怎么就看到挥刀和砸下去」** ⇒ 第①段和第②段**是同一个动作**

实测剪影 IoU：蓄力 0.702 / 命中 0.748。

根因：两条视频都是水平挥砍，唯一差别是扫的方向；
而反向扫到右 = 往身后砍不能用 ⇒ 只能挑前半段 ⇒ 和第①段撞了。

⇒ 第②段重出成**突刺**（深弓步 + 水平直刺）：IoU 掉到 0.595。

> **靠「同一个动作换个方向」做连招区分度，在只能朝一边打的横版里行不通。
> 要换动作类型：挥 / 刺 / 砸。**

**④ 取消窗口要开在收招帧，不能提前**

从 0.16 挪到 0.23。0.16 会跳过收招帧 ⇒ 刀在一帧里从最前端瞬移到下一段的后方蓄力。
现在刀的走向是「前伸 → 回斜垂左下 → 拉右下蓄力 → 举过头」，一路单向。

---

## 八 · 成本

### 8.1 单价

| 项 | 价格 |
|---|---|
| ark seedance（抽卡/探姿势） | **免费额度** |
| GLM cogvideox-flash（idle 类） | **免费** |
| 万相 480P / 5s | **¥1.05** |
| 万相 480P / 2s | **¥0.40** |
| 万相 720P / 5s | ¥2.10 |
| 出图（gpt-image 类中转） | ¥0.15 / 发 |

### 8.2 两条省钱的结论

**① 480P 够用，别默认上 720P**

```
480P 出 632×632，站直 345px，缩到 target=202 只要 0.586 ⇒ 余量 1.7 倍
```

**② 万相 `duration=2` 是收的——这条直接改掉小怪的成本结构**

文档里原本只记了「最长 30s」，**没有下限记录**，于是一直默认最短 5 秒。

```
--res=480P --dur=2 ⇒ 产物 516×774 / 30fps / 60 帧 / 2.000 秒 ≈ ¥0.4
```

小怪一段动作只要 3~4 帧，2 秒 60 帧绰绰有余 ⇒ **一段 0.4 元**，
而且**保住了 `--last`**——免费的 GLM 没这个参数（循环缝 1.54 vs 万相 1.02）。

### 8.3 一个真实的抽卡账（后跳动画，4 发）

| | 最右 Δx | 向左前摇 | 抬升 | 朝向 | 刀高 |
|---|---|---|---|---|---|
| v1 | 4.3% | −7.4% | 45.0% | ✅ 侧面 | ❌ 举过头 |
| v2 | 36.9% | −22.3% | 28.9% | ✅ 侧面 | ❌ 举到头 |
| v3a | 37.5% | −14.9% | 33.0% | ❌ 转正面 | ❌ |
| v3b | 47.6% | **−49.0%** | 18.9% | ❌ 转正面 | ❌ |
| v4 (ark，免费) | 37.0% | −8.3% | 32.7% | ✅ 侧面 | ❌ 平举胸前 |
| **v4 (万相，定稿)** | **36.2%** | **−4.3%** | 16.8% | ✅ 侧面 | ✅ 全程不过腰 |

**总成本：¥1.05**（前面全是免费额度）。

> v3 那两发转正面的病根：提示词里写了「上半身略微向后仰」
> ⇒ 侧视图里「后仰」几乎必然被渲染成「把胸口转向镜头」。
> ⇒ v4 改回中性描述（只说下蹲、双脚原地）+ 在这一步里单独再钉一次「侧面对镜头」⇒ 一次就好。

### 8.4 省钱的操作纪律

```
① 抽卡用免费的（ark 探姿势 / GLM 探静止型），方向定了再用万相出定稿
② 万相一次一发，不散弹
③ 任何消耗额度的生成，发之前先确认
④ 任务 id 立刻落盘
⑤ 提示词的历史版本和每一轮的病因都留着 —— 那是防重犯的
```

---

## 九 · 工具清单

| 工具 | 作用 |
|---|---|
| `gen_video.py` | 图生视频，三个 provider（dashscope / zhipu / ark），支持 `--last` 首尾帧、`--poll` 断点续取 |
| `vid2anim.py` | **核心**：视频 → 序列帧图集。三种挑帧模式、对齐、归一化、冻结 |
| `anim_bench.py` | 判据。上下限全部从已出货游戏量出来 |
| `make_liubai.py` | 立绘 → 留白图（可指定 `--cx` 做位移尾帧） |
| `_contact.py` | 带**帧号**的联络表（别手搓 ffmpeg tile） |
| `_blade_check.py` | 逐帧查「刀有没有变成细线」 |
| `_anim_check.py` | 动作图集自检（朝向 / IoU 区分度 / 贴图与 trim 配对） |
| `artgen/gen.py` | 出图 |
| `artgen/alpha.py` | 黑底抠图（自发光特效用） |
| `artgen/cutout_grey.py` | 中性灰底抠图（角色用，含去底色反解） |

**provider 决定端点 / 请求体 / 轮询格式三样，别只换 model 名。**

---

## 十 · 十条最贵的教训

1. **出任何动画前，先确认源图是哪一张。**（¥6.3 + 一整轮返工）
2. **判据必须是区间**，只有下限的判据会把"角色整个发白"判成达标。
3. **判据要从已出货的产品上量**，不是自己拍。
4. **seedance 做大动作是在重新生成角色**——这是能力问题，提示词修不了。
5. **要姿态，别要成因**。写"有气流"得到"水平飘"，写"发梢指向顶边"才得到竖起。
6. **禁止句单独成段**，混进名词串里权重会被稀释。
7. **`--last` 首尾帧比提示词可靠**。「回到开头的站姿」写三遍模型也不听。
8. **代码位移只能补动画里已有的位移，不能凭空造。**
9. **方向要在切出来的图上量**，原始视频里的运动模糊会骗你。
10. **静默失败是最贵的**——抠图失效、身高恒等于画面高、贴图与 trim 不配对、
    任务 id 丢失，**这四件事一行报错都没有**。每一处都得有自检脚本。

---

## 附：这套方法的适用边界

**适用**：
- 单人 / 小团队，没有美术
- 2D，角色朝向少（横版左右两向最理想）
- 角色风格统一、有一张能定稿的立绘

**不适用**：
- 需要装备可见 / 换装
- 需要 8 方向
- 需要角色间大量交互动作（抱、扛、投技）
- 帧数要求高的格斗游戏（这套出的是 3~6 帧的动作）

**最本质的那条线**：
这套管线的全部价值在于**把"理解画面内容"这件事外包给了模型**，
自己只做机械操作。任何一步一旦需要你去理解并拆解画面，
成本就会回到骨骼动画那个量级——那正是这条路当初绕开的东西。
